<%= form_for [:admin, @package] do  |f| %>
    <% if @package.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@package.errors.count, "error") %> prohibited this user from being saved:</h2>

          <ul>
            <% @package.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>


    <div class="form-group">
      <%= f.text_field :name, {class: 'form-control', placeholder: "Nombre"} %>
    </div>
    <div class="form-group">
      <%= f.text_field :price, {class: 'form-control', placeholder: "Precio"} %>
    </div>
    <div class="form-group">
      <%= f.text_field :classes, {class: 'form-control', placeholder: "Número de clases"} %>
    </div>

    <div class="actions">
      <%= f.submit 'Guardar' , class: "btn btn-primary" %>
    </div>

<% end %>
