<%= form_for [:admin, @location], html: { multipart: true } do  |f| %>
    <% if @location.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@location.errors.count, "error") %> prohibited this user from being saved:</h2>

          <ul>
            <% @location.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>


    <div class="form-group">
      <%= f.text_field :name, {class: 'form-control', placeholder: "Nombre"} %>
    </div>
    <div class="form-group">
      <%= f.text_field :address, {class: 'form-control', placeholder: "Dirección"} %>
    </div>
    <div class="form-group">
      <%= f.text_field :opening_hour, {class: 'form-control', placeholder: "Horario apertura"} %>
    </div>
    <div class="form-group">
      <%= f.text_field :closing_hour, {class: 'form-control', placeholder: "Horario cierre"} %>
    </div>

    <div class="actions">
      <%= f.submit 'Guardar' , class: "btn btn-primary" %>
    </div>

<% end %>
