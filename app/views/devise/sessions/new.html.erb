<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div class="well">
      <h2>Iniciar sesión usuarios</h2>

      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="field">
            <%= f.label "Correo electrónico" %><br />
            <%= f.email_field :email, {class: 'form-control', placeholder: "Correo electrónico", autofocus: true} %>
          </div><br>

          <div class="field">
            <%= f.label "Contraseña" %><br />
            <%= f.password_field :password, {class: 'form-control', placeholder: "Contraseña" , autocomplete: "off"} %>
          </div><br>

          <div class="actions">
            <%= f.submit "Log in", class: "btn btn-primary" %>
          </div>
      <% end %>
    </div>
    <%= render "devise/shared/links" %>
    Si todavía no es miembro descarge la aplicación móvil y unase
  </div>
</div>




