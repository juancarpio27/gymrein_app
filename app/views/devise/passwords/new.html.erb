<div clas="row">
  <div class="col-md-6 col-md-offset-3">
    <div class="well">
      <h2>Olvido su contraseña?</h2>

      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
          <%= devise_error_messages! %>

          <div class="field">
            <%= f.label :email %><br />
            <%= f.email_field :email, {class: 'form-control', placeholder: "Contraseña", autofocus: true} %>
          </div><br>

          <div class="actions">
            <%= f.submit "Enviar información de cambio de contraseña", class: "btn btn-primary" %>
          </div>
      <% end %>
    </div>
    <%= render "devise/shared/links" %>
  </div>
</div>





